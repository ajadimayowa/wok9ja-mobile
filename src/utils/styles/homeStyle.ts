import {ScaledSheet} from 'react-native-size-matters';

const homeStyle = ScaledSheet.create({
    container : {
        flex:1,
        backgroundColor:'#fff',
        justifyContent:'center',
        alignItems:'center'
    }
})

export default homeStyle;